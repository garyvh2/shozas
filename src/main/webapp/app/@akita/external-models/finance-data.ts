import { ID } from '@datorama/akita';

export interface FinanceData {
    termMonth: number[];
    terms: number[];
    image: string;
    id: number;
    name: string;
    commission: number;
    rate: number;
    prima: number;
}
